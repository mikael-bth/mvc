{% extends 'project/base.html.twig' %}

{% block content %}
<div class="project-home">
    <h2>{{ header }}</h2>
    <p>
        <a href="{{ path('project-about') }}">Om projektet</a>
    </p>
    <p>
        <a href="{{ path('project-game-start') }}">Spela poker</a>
    </p>
</div>
{% endblock %}
