{# templates/index.html.twig #}

{% extends 'base.html.twig' %}

{% block content %}
    <h2>{{ header }}</h2>
    <p>
        <a href="{{ path('deck') }}">Deck</a>
        <a href="{{ path('dice-graphic-home') }}">Dice Graphic</a>
        <a href="{{ path('dice-hand-home') }}">Dice Hand</a>
    </p>

{% endblock %}
